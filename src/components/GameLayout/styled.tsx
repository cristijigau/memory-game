import Box, { BoxProps } from '@mui/material/Box';
import { styled } from '@mui/material/styles';

export const MainGameContainer = styled(Box)<BoxProps>(() => ({
  backgroundImage:
    'linear-gradient(to right top, #051937, #123460, #1b538c, #1e74ba, #1297eb)',
}));
